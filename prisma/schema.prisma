generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Champion {
  code Int    @id
  name String
}

model Spell {
  code     Int    @id
  name     String
  cooldown Int
}
